[package]
name = "cdDB"
version = "0.1.0"
edition = "2024"

[dependencies]
ahash = { version = "0.8", features = ["serde"] }
crossbeam = "0.8"
serde = { version = "1.0", features = ["derive"] }
bincode = "1.3"


[workspace]
members = [
    ".",
    "tests/cdDB-test",
]
